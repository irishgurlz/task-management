model Project {
  id          Int    @id @default(autoincrement())
  name        String
  description String

  created_by Int
  creator    User @relation(fields: [created_by], references: [id])

  tasks Task[]

  @@map("project")
}
