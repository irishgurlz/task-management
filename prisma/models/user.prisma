model User {
  id       Int    @id @default(autoincrement())
  name     String
  email    String @unique
  password String
  role     Role   @default(user)

  projects Project[]
  tasks    Task[]
  logs     AuditLog[]

  @@map("user")
}

enum Role {
  admin
  user
}
